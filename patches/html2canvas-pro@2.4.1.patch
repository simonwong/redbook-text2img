diff --git a/dist/html2canvas-pro.cjs b/dist/html2canvas-pro.cjs
index 8dcf7cac545d51a799c3e490b18bb2d11389b996..91f274fcf7698c4ce2965227c1744a827ecdde0d 100644
--- a/dist/html2canvas-pro.cjs
+++ b/dist/html2canvas-pro.cjs
@@ -9734,20 +9734,26 @@ var BackgroundRenderer = class {
 		if (rx > 0 && ry > 0) {
 			const cacheKey = `rg|${Math.round(x)}x${Math.round(y)}|${Math.round(rx)}x${Math.round(ry)}|${JSON.stringify(backgroundImage.stops)}`;
 			let pattern = this.patternCache.get(cacheKey);
+			// Patched (redbook-text2img): the upstream code rendered the ellipse into a
+			// max(rx, ry) square and then scaled Y by ry / rx. When ry > rx that stretches the
+			// gradient past the square's edge, so it gets cut off before fading out and leaves a
+			// hard horizontal seam (e.g. tall 9:16 cards). Draw a circle of radius rx into a
+			// 2rx × 2ry box scaled by ry / rx instead, and fill the rest of the area with the last
+			// color stop the way the browser extends a radial gradient beyond its ending shape.
+			const stops = processColorStops(backgroundImage.stops, rx * 2);
+			const lastStop = stops[stops.length - 1];
 			if (!pattern) {
 				const ownerDocument = this.canvas.ownerDocument ?? document;
-				const size = Math.ceil(Math.max(rx, ry) * 2);
 				const offscreen = ownerDocument.createElement("canvas");
-				offscreen.width = size;
-				offscreen.height = size;
+				offscreen.width = Math.ceil(rx * 2);
+				offscreen.height = Math.ceil(ry * 2);
 				const offCtx = offscreen.getContext("2d");
 				if (offCtx) {
-					const offRadius = Math.max(rx, ry);
-					const gradient = offCtx.createRadialGradient(offRadius, offRadius, 0, offRadius, offRadius, offRadius);
-					processColorStops(backgroundImage.stops, offRadius * 2).forEach((s) => gradient.addColorStop(s.stop, asString(s.color)));
+					const gradient = offCtx.createRadialGradient(rx, rx, 0, rx, rx, rx);
+					stops.forEach((s) => gradient.addColorStop(s.stop, asString(s.color)));
 					offCtx.fillStyle = gradient;
 					if (rx !== ry) offCtx.scale(1, ry / rx);
-					offCtx.fillRect(0, 0, offRadius * 2, offRadius * 2);
+					offCtx.fillRect(0, 0, rx * 2, rx * 2);
 					pattern = this.ctx.createPattern(offscreen, "no-repeat");
 					this.patternCache.set(cacheKey, pattern);
 				}
@@ -9756,9 +9762,13 @@ var BackgroundRenderer = class {
 				this.path(path);
 				this.ctx.save();
 				this.ctx.clip();
-				this.ctx.translate(left + x - Math.max(rx, ry), top + y - Math.max(rx, ry));
+				if (lastStop) {
+					this.ctx.fillStyle = asString(lastStop.color);
+					this.ctx.fill();
+				}
+				this.ctx.translate(left + x - rx, top + y - ry);
 				this.ctx.fillStyle = pattern;
-				this.ctx.fillRect(0, 0, Math.max(rx, ry) * 2, Math.max(rx, ry) * 2);
+				this.ctx.fillRect(0, 0, rx * 2, ry * 2);
 				this.ctx.restore();
 			}
 		}
@@ -10721,10 +10731,10 @@ var CanvasRenderer = class CanvasRenderer {
 					this.ctx.clip();
 					this.path(shadowPaintingArea);
 				}
-				this.ctx.shadowOffsetX = shadow.offsetX.number + maskOffset;
-				this.ctx.shadowOffsetY = shadow.offsetY.number;
+				this.ctx.shadowOffsetX = (shadow.offsetX.number + maskOffset) * this.options.scale;
+				this.ctx.shadowOffsetY = shadow.offsetY.number * this.options.scale;
 				this.ctx.shadowColor = asString(shadow.color);
-				this.ctx.shadowBlur = shadow.blur.number;
+				this.ctx.shadowBlur = shadow.blur.number * this.options.scale;
 				this.ctx.fillStyle = shadow.inset ? asString(shadow.color) : "rgba(0,0,0,1)";
 				this.ctx.fill();
 				this.ctx.restore();
diff --git a/dist/html2canvas-pro.esm.js b/dist/html2canvas-pro.esm.js
index 4e795fca83ac33075ee6c3737400718753bd3f63..685e8fe51bb6cf1fa6a66c3a90fed6d701ef0929 100644
--- a/dist/html2canvas-pro.esm.js
+++ b/dist/html2canvas-pro.esm.js
@@ -9730,20 +9730,26 @@ var BackgroundRenderer = class {
 		if (rx > 0 && ry > 0) {
 			const cacheKey = `rg|${Math.round(x)}x${Math.round(y)}|${Math.round(rx)}x${Math.round(ry)}|${JSON.stringify(backgroundImage.stops)}`;
 			let pattern = this.patternCache.get(cacheKey);
+			// Patched (redbook-text2img): the upstream code rendered the ellipse into a
+			// max(rx, ry) square and then scaled Y by ry / rx. When ry > rx that stretches the
+			// gradient past the square's edge, so it gets cut off before fading out and leaves a
+			// hard horizontal seam (e.g. tall 9:16 cards). Draw a circle of radius rx into a
+			// 2rx × 2ry box scaled by ry / rx instead, and fill the rest of the area with the last
+			// color stop the way the browser extends a radial gradient beyond its ending shape.
+			const stops = processColorStops(backgroundImage.stops, rx * 2);
+			const lastStop = stops[stops.length - 1];
 			if (!pattern) {
 				const ownerDocument = this.canvas.ownerDocument ?? document;
-				const size = Math.ceil(Math.max(rx, ry) * 2);
 				const offscreen = ownerDocument.createElement("canvas");
-				offscreen.width = size;
-				offscreen.height = size;
+				offscreen.width = Math.ceil(rx * 2);
+				offscreen.height = Math.ceil(ry * 2);
 				const offCtx = offscreen.getContext("2d");
 				if (offCtx) {
-					const offRadius = Math.max(rx, ry);
-					const gradient = offCtx.createRadialGradient(offRadius, offRadius, 0, offRadius, offRadius, offRadius);
-					processColorStops(backgroundImage.stops, offRadius * 2).forEach((s) => gradient.addColorStop(s.stop, asString(s.color)));
+					const gradient = offCtx.createRadialGradient(rx, rx, 0, rx, rx, rx);
+					stops.forEach((s) => gradient.addColorStop(s.stop, asString(s.color)));
 					offCtx.fillStyle = gradient;
 					if (rx !== ry) offCtx.scale(1, ry / rx);
-					offCtx.fillRect(0, 0, offRadius * 2, offRadius * 2);
+					offCtx.fillRect(0, 0, rx * 2, rx * 2);
 					pattern = this.ctx.createPattern(offscreen, "no-repeat");
 					this.patternCache.set(cacheKey, pattern);
 				}
@@ -9752,9 +9758,13 @@ var BackgroundRenderer = class {
 				this.path(path);
 				this.ctx.save();
 				this.ctx.clip();
-				this.ctx.translate(left + x - Math.max(rx, ry), top + y - Math.max(rx, ry));
+				if (lastStop) {
+					this.ctx.fillStyle = asString(lastStop.color);
+					this.ctx.fill();
+				}
+				this.ctx.translate(left + x - rx, top + y - ry);
 				this.ctx.fillStyle = pattern;
-				this.ctx.fillRect(0, 0, Math.max(rx, ry) * 2, Math.max(rx, ry) * 2);
+				this.ctx.fillRect(0, 0, rx * 2, ry * 2);
 				this.ctx.restore();
 			}
 		}
@@ -10717,10 +10727,10 @@ var CanvasRenderer = class CanvasRenderer {
 					this.ctx.clip();
 					this.path(shadowPaintingArea);
 				}
-				this.ctx.shadowOffsetX = shadow.offsetX.number + maskOffset;
-				this.ctx.shadowOffsetY = shadow.offsetY.number;
+				this.ctx.shadowOffsetX = (shadow.offsetX.number + maskOffset) * this.options.scale;
+				this.ctx.shadowOffsetY = shadow.offsetY.number * this.options.scale;
 				this.ctx.shadowColor = asString(shadow.color);
-				this.ctx.shadowBlur = shadow.blur.number;
+				this.ctx.shadowBlur = shadow.blur.number * this.options.scale;
 				this.ctx.fillStyle = shadow.inset ? asString(shadow.color) : "rgba(0,0,0,1)";
 				this.ctx.fill();
 				this.ctx.restore();
diff --git a/dist/html2canvas-pro.js b/dist/html2canvas-pro.js
index 6856b82c590db9541cba6e84a9f4111ce285c7da..4cbb4c25efa4ba1367e43bffb1ba8122ca99deda 100644
--- a/dist/html2canvas-pro.js
+++ b/dist/html2canvas-pro.js
@@ -9737,20 +9737,26 @@
 			if (rx > 0 && ry > 0) {
 				const cacheKey = `rg|${Math.round(x)}x${Math.round(y)}|${Math.round(rx)}x${Math.round(ry)}|${JSON.stringify(backgroundImage.stops)}`;
 				let pattern = this.patternCache.get(cacheKey);
+				// Patched (redbook-text2img): the upstream code rendered the ellipse into a
+				// max(rx, ry) square and then scaled Y by ry / rx. When ry > rx that stretches the
+				// gradient past the square's edge, so it gets cut off before fading out and leaves a
+				// hard horizontal seam (e.g. tall 9:16 cards). Draw a circle of radius rx into a
+				// 2rx × 2ry box scaled by ry / rx instead, and fill the rest of the area with the last
+				// color stop the way the browser extends a radial gradient beyond its ending shape.
+				const stops = processColorStops(backgroundImage.stops, rx * 2);
+				const lastStop = stops[stops.length - 1];
 				if (!pattern) {
 					const ownerDocument = this.canvas.ownerDocument ?? document;
-					const size = Math.ceil(Math.max(rx, ry) * 2);
 					const offscreen = ownerDocument.createElement("canvas");
-					offscreen.width = size;
-					offscreen.height = size;
+					offscreen.width = Math.ceil(rx * 2);
+					offscreen.height = Math.ceil(ry * 2);
 					const offCtx = offscreen.getContext("2d");
 					if (offCtx) {
-						const offRadius = Math.max(rx, ry);
-						const gradient = offCtx.createRadialGradient(offRadius, offRadius, 0, offRadius, offRadius, offRadius);
-						processColorStops(backgroundImage.stops, offRadius * 2).forEach((s) => gradient.addColorStop(s.stop, asString(s.color)));
+						const gradient = offCtx.createRadialGradient(rx, rx, 0, rx, rx, rx);
+						stops.forEach((s) => gradient.addColorStop(s.stop, asString(s.color)));
 						offCtx.fillStyle = gradient;
 						if (rx !== ry) offCtx.scale(1, ry / rx);
-						offCtx.fillRect(0, 0, offRadius * 2, offRadius * 2);
+						offCtx.fillRect(0, 0, rx * 2, rx * 2);
 						pattern = this.ctx.createPattern(offscreen, "no-repeat");
 						this.patternCache.set(cacheKey, pattern);
 					}
@@ -9759,9 +9765,13 @@
 					this.path(path);
 					this.ctx.save();
 					this.ctx.clip();
-					this.ctx.translate(left + x - Math.max(rx, ry), top + y - Math.max(rx, ry));
+					if (lastStop) {
+						this.ctx.fillStyle = asString(lastStop.color);
+						this.ctx.fill();
+					}
+					this.ctx.translate(left + x - rx, top + y - ry);
 					this.ctx.fillStyle = pattern;
-					this.ctx.fillRect(0, 0, Math.max(rx, ry) * 2, Math.max(rx, ry) * 2);
+					this.ctx.fillRect(0, 0, rx * 2, ry * 2);
 					this.ctx.restore();
 				}
 			}
@@ -10724,10 +10734,10 @@
 						this.ctx.clip();
 						this.path(shadowPaintingArea);
 					}
-					this.ctx.shadowOffsetX = shadow.offsetX.number + maskOffset;
-					this.ctx.shadowOffsetY = shadow.offsetY.number;
+					this.ctx.shadowOffsetX = (shadow.offsetX.number + maskOffset) * this.options.scale;
+					this.ctx.shadowOffsetY = shadow.offsetY.number * this.options.scale;
 					this.ctx.shadowColor = asString(shadow.color);
-					this.ctx.shadowBlur = shadow.blur.number;
+					this.ctx.shadowBlur = shadow.blur.number * this.options.scale;
 					this.ctx.fillStyle = shadow.inset ? asString(shadow.color) : "rgba(0,0,0,1)";
 					this.ctx.fill();
 					this.ctx.restore();
diff --git a/dist/lib/render/canvas/canvas-renderer.js b/dist/lib/render/canvas/canvas-renderer.js
index 64a5ca982980b201023788d35b67935be12b15f0..fd74a1e057b69267f8438e3b3338099e9f3c3eb5 100644
--- a/dist/lib/render/canvas/canvas-renderer.js
+++ b/dist/lib/render/canvas/canvas-renderer.js
@@ -254,10 +254,10 @@ class CanvasRenderer {
                     this.ctx.clip();
                     this.path(shadowPaintingArea);
                 }
-                this.ctx.shadowOffsetX = shadow.offsetX.number + maskOffset;
-                this.ctx.shadowOffsetY = shadow.offsetY.number;
+                this.ctx.shadowOffsetX = (shadow.offsetX.number + maskOffset) * this.options.scale;
+                this.ctx.shadowOffsetY = shadow.offsetY.number * this.options.scale;
                 this.ctx.shadowColor = (0, color_utilities_1.asString)(shadow.color);
-                this.ctx.shadowBlur = shadow.blur.number;
+                this.ctx.shadowBlur = shadow.blur.number * this.options.scale;
                 this.ctx.fillStyle = shadow.inset ? (0, color_utilities_1.asString)(shadow.color) : 'rgba(0,0,0,1)';
                 this.ctx.fill();
                 this.ctx.restore();
