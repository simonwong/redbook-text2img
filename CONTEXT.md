# 图片样式

图片样式上下文定义从一键主题到最终预览和导出外观之间的语言。它让用户通过有限、可靠的选择制作图片，同时把 CSS 和导出兼容性留在系统内部。

## Language

**主题（Theme）**：
有名称和说明的完整样式配置，用作一键选择的起点。8 个内置主题彼此同级。
_Avoid_: 模板、样式包、场景主题

**内置主题（Built-in Theme）**：
产品随版本提供的主题。内置主题可以随产品升级，但必须保留稳定标识和可迁移语义。
_Avoid_: 默认配置、系统样式

**样式配置（Style Configuration）**：
用户可以理解和修改的有限视觉选择集合，包括背景、排版、标题和封面版式。它不包含原始 CSS。
_Avoid_: 调整项、CSS 配置、完整样式

**排版（Typography Configuration）**：
密度和字体的组合。密度有紧凑、较紧、正常、较松、宽松五档；字体只有跟随主题、无衬线、衬线。旧值只用于迁移，不在界面中显示。
_Avoid_: 系统默认字体、楷体、圆体、缺少可靠中文回退的等宽字体

**样式基础（Style Foundation）**：
把样式配置变成可靠图片外观的内部视觉规则。它包含语义颜色、间距、字体回退和导出兼容规则，不向用户开放。
_Avoid_: 基础主题、底层配置

**画布背景（Canvas Background）**：
整张图片的底色或受控图案。用户只能选择内置背景方案或合法的 6 位十六进制纯色；不能输入 CSS、渐变或远程资源。纯色会自动采用可读的浅色或深色语义基础。
_Avoid_: 自定义 CSS、背景代码、背景 URL

**正文标题（Body Heading）**：
只出现在正文图片中的 Markdown 标题。正文标题对齐不改变封面标题的位置。
_Avoid_: 全局标题、封面标题

**标题装饰（Heading Decoration）**：
只作用于标题文字的无、直线、波浪或高亮效果。装饰颜色不改变 Markdown 高亮、链接、列表或引用的语义颜色。
_Avoid_: 强调色、品牌色

**封面版式（Cover Layout）**：
封面内容的位置规则，当前包含居中海报、左上和左下。它不覆盖正文标题配置。
_Avoid_: 封面主题、标题对齐

**渲染样式（Render Style）**：
针对封面或正文解析完成、可以直接用于预览和导出的最终图片外观。它不是用户选择或持久化数据。
_Avoid_: 调整后样式、生成样式

**自定义配置（Custom Configuration）**：
用户命名并保存的样式配置。它与内置主题共享相同配置能力，但不属于当前交付范围。
_Avoid_: 自定义主题、用户主题

**卡片标记（Card Mark）**：
随图片导出的署名和正文页码偏好。卡片标记独立于主题和样式配置，切换或重置主题时保持不变。
_Avoid_: 主题页脚、水印样式
